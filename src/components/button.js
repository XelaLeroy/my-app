import React from "react";
import './Button.css'

const Button = ({ jouer }) =>  (
        <button onClick={jouer}
        >
          Viellit les tous de 2 ans
          </button>
    )



export default Button